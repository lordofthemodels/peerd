// @ts-check
// Store actor entry: Contributor Metrics is absent from this target, so the
// dedicated worker cannot import or execute its semantic projection.

import { startActorWorker } from './actor-worker-runtime.js';

startActorWorker();
